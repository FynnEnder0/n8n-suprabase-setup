version: '3.8'

# Main orchestrator file - includes all modular compose files
# For Hostinger deployment, ensure network is created first: docker network create shared-network

include:
  - docker-compose.postgres.yml
  - docker-compose.n8n.yml
  - docker-compose.supabase.yml
  - docker-compose.traefik.yml
